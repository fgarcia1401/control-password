<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke
        android:width="0dp"
        android:color="@color/color_accent_disable" />
    <corners android:radius="10dp" />
    <solid android:color="@color/color_accent_disable" />
</shape>